schema_version: 1

context:
  name: rootstock
  version: "1.6.1"
  python_min: '3.11'

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/rootstock-${{ version }}.tar.gz
  sha256: d181e4c5c4b1c4b92841df93042d3412690372a12cb32209d22ccf15ea9004de

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - rootstock = rootstock.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - ase >=3.22
    - numpy >=1.24
    - packaging >=21.0
    - tomli-w >=1.0
tests:
  - python:
      imports:
        - rootstock
      pip_check: true
      python_version: ${{ python_min }}
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - rootstock --help

about:
  summary: MLIP calculators with isolated Python environments
  license: MIT
  license_file: LICENSE.md
  homepage: https://pypi.org/project/rootstock/

extra:
  recipe-maintainers:
    - jan-janssen
