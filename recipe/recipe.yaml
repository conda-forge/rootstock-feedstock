schema_version: 1

context:
  name: rootstock
  version: 0.9.8
  python_min: '3.11'

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/rootstock-${{ version }}.tar.gz
  sha256: bdd1ffdd99d5d1fdd11f111b043f1a8aef23d8f5d9fdeda96534930110c93f88

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - rootstock = rootstock.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - ase >=3.22
    - numpy >=1.24
    - packaging >=21.0
tests:
  - python:
      imports:
        - rootstock
      pip_check: true
      python_version: ${{ python_min }}
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - rootstock --help

about:
  summary: MLIP calculators with isolated Python environments
  license: MIT
  license_file: LICENSE.md
  homepage: https://pypi.org/project/rootstock/

extra:
  recipe-maintainers:
    - jan-janssen
