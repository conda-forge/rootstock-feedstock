{% set name = "rootstock" %}
{% set version = "0.7.1" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/rootstock-{{ version }}.tar.gz
  sha256: ec04b63cdf08ad1e0b9c78c5e63f66bdd70d89e6a7a110ab63fa447fa296ebdc

build:
  noarch: python
  entry_points:
    - rootstock = rootstock.cli:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - ase >=3.22
    - numpy >=1.24
    - packaging >=21.0

test:
  imports:
    - rootstock
  commands:
    - pip check
    - rootstock --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/rootstock/
  summary: MLIP calculators with isolated Python environments
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - jan-janssen
